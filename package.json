{
  "name": "codemod-storybook-addon-info",
  "version": "1.0.0",
  "description": "A codemod to convert deprecated `withInfo()` decorator to `info` parameter",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "jscodeshift": "^0.7.0"
  },
  "devDependencies": {
    "jest": "^24.9.0"
  }
}
